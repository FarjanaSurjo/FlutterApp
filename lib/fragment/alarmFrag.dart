import 'package:flutter/material.dart';

class alarmFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Alarm Fragment!"),
      ),
    );
  }
}
