import 'package:flutter/material.dart';

class personFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Person Fragment!"),
      ),
    );
  }
}
