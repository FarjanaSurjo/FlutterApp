import 'package:flutter/material.dart';

class balanceFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Balance Fragment!"),
      ),
    );
  }
}
